<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="a.b.c">
	<!-- <select id="getTime" resultType="string">
		select sysdate from dual
	</select>
	
	<insert id="insertMember">
	insert into tbl_mymember (userid, userPw, userName, email) 
	values (#{userid}, #{userPw}, #{userName}, #{email}) 
	</insert>
	
	<select id="listMember" resultType="MyMemberVO">
	select * from tbl_mymember
	</select>
	
	<select id="selectById" resultType="MyMemberVO">
	select * from tbl_mymember where userid = #{userid}
	</select>
	
	<update id="updatePw">
	update tbl_mymember set userPw=#{newPw} where userid = #{vo.userid} and userPw = #{vo.userPw}
	</update> -->
</mapper>